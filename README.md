# Newton's Laws of Motion

An interactive demon of Newton's laws of motion.

Built with React, TypeScript, and Vite.

## Development

Prerequisites: Node.js and pnpm.

```bash
# install dependencies
pnpm install

# start dev server
pnpm dev

# run tests
pnpm test

# build
pnpm build

# lint
pnpm lint
```
